package com.hazee.hypertp.listener;

import org.bukkit.event.Listener;
import com.hazee.hypertp.HyperTP;

public class GUIListener implements Listener {
    private final HyperTP plugin;
    public GUIListener(HyperTP plugin) { this.plugin = plugin; }
    // Implement InventoryClick handling based on GUI config files (left as extendable skeleton)
}
